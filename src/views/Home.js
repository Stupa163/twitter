import React from 'react';
import Navbar from "../components/Navbar";

const Home = (props) => {
    return (
        <div>
            <Navbar/>
            <div>Home</div>
        </div>
    )
};

export default Home;
